To connect in Database add '.env' in your project with:

example:

DB_DATABASE= (mysql)
DB_DRIVER= (pymysql)
DB_USERNAME= (root)
DB_PASSWORD= (password)
DB_URL= (localhost)
DB_PORT= (port)
DB_DB= (database_name)

without parentheses
